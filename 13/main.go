package main

import "fmt"

func main(){
	data := []int{1,2}
	data[0], data[1] = data[1], data[0]
	fmt.Print(data)
	one:=1
	tow:=2
	one, tow = tow,one
	fmt.Printf("\none: %d, two: %d\n",one,tow)
	// да я думаю тут и рассказывать ничего не надо, просто особенность языка
	// фича что с нее взять
	// Множественное присваивание позволяет обменять значения между переменными
	// без необходимости создавать временные переменные.
}